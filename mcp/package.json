{
	"name": "bc-canvas-mcp",
	"private": true,
	"version": "0.0.1",
	"description": "Local MCP server over committed Bounded Context Canvas files",
	"type": "module",
	"bin": {
		"bc-canvas-mcp": "./dist/server.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "node build.js",
		"test": "vitest run",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@modelcontextprotocol/server": "^2.0.0",
		"zod": "^4.2.0"
	},
	"devDependencies": {
		"@modelcontextprotocol/client": "^2.0.0",
		"@types/node": "^26.2.0",
		"esbuild": "^0.27.7",
		"typescript": "^6.0.3",
		"vitest": "^4.1.10"
	}
}
