### Anywhere else

Two rules. Raw HTML has to survive the pipeline — `remark-rehype` and
`rehype-stringify` both take `allowDangerousHtml: true` — and the sheet's CSS
has to reach the page one of two ways:
